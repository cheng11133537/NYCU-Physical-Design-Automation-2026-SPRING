Compilation:
Simply type `make` in the terminal to build the executable. This will generate a binary file named `Lab1`.

Execution:
./Lab1 [input file name] [output file name]

Example:
./Lab1 input.txt output.txt

To clean the compiled files, type `make clean`.